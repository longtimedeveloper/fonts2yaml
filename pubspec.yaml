name: fonts2yaml
description: Dart command-line tool that updates the fonts section in the Flutter pubspec.yaml file based on the font files in your project.
dart pub add mockitoversion: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies: 
  get_it: ^7.1.1
  mockito: ^5.0.7

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4

executables:
  fonts2yaml:
